---
import Layout from "../layouts/Layout.astro";
const title = "faith.tools - coming soon";
const description =
  "Equipping believers with technology to help them live their God-given calling every single day, together";
---

<Layout
  title={title}
  description={description}
  bodyClasses="grid grid-rows-[1fr_auto] gap-4"
>
  <main
    id="main"
    class="container p-6 flex flex-col justify-center gap-4 items-center"
  >
    <svg
      width={128}
      viewBox="0 0 512 231"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
    >
      <path
        class="solid-line"
        d="M18 119.305C158.8 265.669 353.36 195.055 494.16 32"
        stroke="currentColor"
        stroke-width="36"
        stroke-linecap="round"></path>
      <path
        class="dotted-line"
        d="M19 120.5C159.8 -25.864 353.36 35.9452 494.16 199"
        stroke="currentColor"
        stroke-opacity="0.5"
        stroke-width="36"
        stroke-linecap="round"
        stroke-dasharray="1 56.28"></path>

      <style>
        :root {
          --animation-duration: 750ms;
          --second-line-delay: 300ms;
          --animation-transition: cubic-bezier(0.5, 0.07, 0.31, 1);
        }

        .solid-line {
          stroke-dasharray: 1000;
          stroke-dashoffset: 1000;
          animation: line-animation var(--animation-duration)
            var(--animation-transition) forwards;
        }

        .dotted-line {
          stroke-dasharray: 1000;
          stroke-dashoffset: 1000;
          animation-direction: reverse;
          animation: line-animation var(--animation-duration)
            var(--animation-transition) forwards;
          animation-delay: var(--second-line-delay);
        }

        @keyframes line-animation {
          to {
            stroke-dashoffset: 0;
          }
        }
      </style>
    </svg>

    <h1 class="flex items-center gap-2 text-4xl font-bold max-sm:flex-col">
      faith.tools <div class="badge bg-blue-100 py-1 h-fit px-3 text-blue-800">
        coming soon
      </div>
    </h1>
    <p class="balanced m-0 max-w-[65ch] text-center text-slate-800">
      We're building a collection of the best digital services and apps for
      Christians, as well as the faces and stories of the
      faithful people behind those tools.
    </p>

    <section class="flex flex-col items-center gap-4 mt-4">
      <div class="flex justify-center gap-3">
        <a href="/add/tool" target="_blank" class="btn btn-neutral"
        >Submit a Tool</a
        >
        <a href="/add/creator" target="_blank" class="btn">Submit a Creator</a>
      </div>
      <p class="balanced text-slate-500 m-0 max-w-[65ch] text-sm text-center">Do you make a digital product or service that could help build the Kingdom of God?</p>
    </section>
  </main>

  <footer class="container mx-auto p-6">
    <p class="text-center m-0 text-sm text-slate-500">
      Created by <a
        class="inherit"
        href="https://cameronpak.com"
        target="_blank">Cameron Pak</a
      >
    </p>
  </footer>
</Layout>
